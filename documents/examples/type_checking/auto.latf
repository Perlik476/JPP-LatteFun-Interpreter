auto f(int x) {
    return lambda (int y) -> int {
        return x + y;
    };
}

int main() {
    auto g = f(1);
    println(g(10));

    g = lambda (int ref z) -> int . z + 1;
    println(g(10));

    return 0;
}