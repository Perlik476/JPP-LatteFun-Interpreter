[(int) -> int] f(bool b) {
    if (b) {
        return lambda (int x) -> int . x + 1;
    }
}

auto g() {
    return 1;
    return false;
}

auto fib(int n) {
    if (n < 2) {
        return n;
    }
    else {
        return fib(5);
    }
    return fib(1);
}

int main() {
    println((f(true))(0));
    
    return 0;
}