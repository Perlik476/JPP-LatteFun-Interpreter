int fib(int n) {
    bool check(int n) {
        return n < 2;
    }

    if (check(n)) {
        return n;
    }

    return fib(n - 1) + fib(n - 2);
}

int main() {
    int x = fib(10);
    print(x); // 55

    return 0;
}