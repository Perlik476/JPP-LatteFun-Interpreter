[(int) -> int] get_mul_two() {
    return lambda (int x) -> int . x * 2;
}

int main() {
    println((lambda (int x, int y) -> int . x + y)(2, 3)); // 5

    println((get_mul_two())(10)); // 20

    return 0;
}