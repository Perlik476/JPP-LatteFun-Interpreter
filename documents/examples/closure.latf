[(int) -> int] f(int x) {
    return lambda (int y) -> int {
        x++;
        return x + y;
    };
}

int main() {
    [(int) -> int] g = f(5);
    int x;
    int y;
    int z;
    print(g(1)); // 7
    print(g(1)); // 8
    return 0;
}